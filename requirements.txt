# time-manager requirements
#
# For development, use: pip install -e ".[dev]"
# For all optional features: pip install -e ".[all]"
# See pyproject.toml for full dependency specification.

# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
toml>=0.10.0

# Chrony SHM integration (required for chrony output)
sysv_ipc>=1.1.0

# IRI-2020 ionospheric model (optional, improves propagation delay estimates)
iricore>=1.9.0

# Optional: ZeroMQ publisher (install with: pip install -e ".[zmq]")
# pyzmq>=22.0.0
